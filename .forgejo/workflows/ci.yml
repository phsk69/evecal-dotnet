# ci pipeline — makes sure nothing is bricked before we merge no cap 🔥
name: ci

on:
  push:
    branches: [develop, master]
  pull_request:
    branches: [develop, master]

jobs:
  build-and-test:
    runs-on: linux
    steps:
      - name: yoink the code 📥
        uses: actions/checkout@v4

      - name: build the whole thing bestie 🏗️
        run: dotnet build --configuration Release

      - name: run the tests — check all the vibes 🧪
        run: dotnet test --configuration Release --no-build
        env:
          MATRIX_WEBHOOK_URL: ${{ secrets.MATRIX_WEBHOOK_URL }}
