# EVE Developer Application Client ID
# Get this from https://developers.eveonline.com/
EVE_CLIENT_ID=your_client_id_here

# OAuth callback URL (default is fine for local development)
EVE_CALLBACK_URL=http://localhost:8080/callback

# How often to refresh calendar data from ESI (in minutes)
CALENDAR_REFRESH_MINUTES=15

# Optional: Provide your own encryption key for token storage
# If not set, one will be auto-generated and stored in data/encryption.key
# TOKEN_ENCRYPTION_KEY=your_base64_encoded_32_byte_key

# Matrix webhook notifications — warnings and errors go to the chat fr 📨
# Set your hookshot webhook URL to enable (auth token is part of the URL)
# MATRIX_WEBHOOK_URL=https://hookshot.example.com/webhook/abc123

# Forgejo Configuration — used by just re-release for API calls 🏠
# API URL constructed as ${FORGEJO_BASE_URL}/api/v1
# FORGEJO_BASE_URL=https://forgejo.something.tld
# FORGEJO_TOKEN=your_forgejo_personal_access_token

# GitHub PAT — used by just re-release for GitHub mirror cleanup 🪞
# GH_PAT=your_github_personal_access_token
